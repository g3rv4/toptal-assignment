(function (define) {
    define(
        ['angular'],
        function () {
            var LoginController = function () {
            };

            return [LoginController];
        }
    );
}(define));